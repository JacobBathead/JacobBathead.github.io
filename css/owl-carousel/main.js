//Calling the plugin
$(document).ready(function(){
  $(".owl-carousel").owlCarousel({
    
	  items : 1,
	  autoHeight : true,
	  autoplay:true,
	  autoplayTimeout:5000,
	  autoplayHoverPause:true,
	  loop:true,
	  
	
});
});
